<resources>
    <!-- Minimal window theme; all real theming is done by SkyTheme in Compose. -->
    <style name="Theme.WeatherApp" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/sky_window_background</item>
    </style>
</resources>