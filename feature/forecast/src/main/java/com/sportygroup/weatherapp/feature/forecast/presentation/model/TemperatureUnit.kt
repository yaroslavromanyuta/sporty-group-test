package com.sportygroup.weatherapp.feature.forecast.presentation.model

/** Temperature unit chosen in the UI. Drives presentation-layer formatting only. */
enum class TemperatureUnit {
    CELSIUS,
    FAHRENHEIT,
}